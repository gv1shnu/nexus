use_default_settings: true

server:
  # Placeholder replaced at container start by the SEARXNG_SECRET env var
  # (see the official image entrypoint). Locally it stays as-is, which is fine.
  secret_key: "ultrasecretkey"
  limiter: false

search:
  formats:
    - html
    - json
